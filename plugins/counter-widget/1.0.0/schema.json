{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Counter Widget Config",
  "description": "Configuration schema for the Counter Widget plugin. Validated against config_overlay when installing or updating the plugin assignment.",
  "type": "object",
  "properties": {
    "initial_value": {
      "type": "integer",
      "description": "Initial counter value on plugin start.",
      "default": 0,
      "minimum": 0,
      "maximum": 1000000
    },
    "step": {
      "type": "integer",
      "description": "Increment step size per button click.",
      "default": 1,
      "minimum": 1,
      "maximum": 100
    },
    "label": {
      "type": "string",
      "description": "Display title shown inside the widget.",
      "default": "Counter",
      "maxLength": 32
    }
  },
  "required": [],
  "additionalProperties": false
}
